# TTS语音评测系统依赖包

# Web框架
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# 语音处理
librosa>=0.10.0
soundfile>=0.12.0

# 语音识别 (FunASR)
funasr>=1.0.0
torch>=2.0.0
torchaudio>=2.0.0

# 模型下载
modelscope[audio]>=1.9.0

# 文本处理和评测
jieba>=0.42.1
editdistance>=0.6.0

# 数据处理
numpy>=1.24.0
pandas>=2.0.0

# 异步支持
aiofiles>=23.0.0

# WeTextProcessing文本标准化模块
pynini==2.1.5

# 注意: 以下是Python标准库模块，不需要安装
# pathlib, json, hashlib, datetime, logging, asyncio
# concurrent.futures, dataclasses, typing
